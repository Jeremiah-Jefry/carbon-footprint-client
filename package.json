{
  "name": "carbon-footprint-tracker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @carbon/emissions-engine && npm run build -w @carbon/api && npm run build -w vite-app",
    "test": "npm run test --workspaces"
  }
}
